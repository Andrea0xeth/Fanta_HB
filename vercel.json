{
  "buildCommand": "pnpm install && pnpm --filter @30diciaccio/web build",
  "outputDirectory": "dist",
  "rootDirectory": "apps/web",
  "framework": "vite",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

